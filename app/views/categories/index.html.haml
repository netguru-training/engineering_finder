%h1 Categories view


- if categories.empty?
  %h2
    %center
      We're sorry, there are no categories added (yet)!!!
- else
  - categories.each do |category|
    .row
      .col-md-8
        = category.name
      .col-md-2
        Theses in this category:
        = category.theses.count
      .col-md-2
        = link_to "Show", category_path(category), class: "btn btn-info"
